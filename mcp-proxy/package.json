{
  "name": "@basira/mcp",
  "version": "1.0.0",
  "description": "Local MCP server that proxies to Basira's hosted MCP endpoint.",
  "bin": {
    "basira-mcp": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "type": "module",
  "files": ["dist", "README.md"],
  "scripts": {
    "build": "tsc",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0"
  },
  "devDependencies": {
    "tsx": "^4.20.6",
    "typescript": "^5.7.0"
  }
}
